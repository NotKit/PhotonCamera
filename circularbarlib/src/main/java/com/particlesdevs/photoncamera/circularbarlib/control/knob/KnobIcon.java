package com.particlesdevs.photoncamera.circularbarlib.control.knob;

/**
 * An icon a knob item asks for instead of a text label.  The model names the
 * icon; the knob widget resolves it to a drawable of its own.
 */
public enum KnobIcon {
    NONE,
    FOCUS_NEAR,
    FOCUS_FAR
}
