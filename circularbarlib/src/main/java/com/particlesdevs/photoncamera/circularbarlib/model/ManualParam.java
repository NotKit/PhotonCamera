package com.particlesdevs.photoncamera.circularbarlib.model;

/** The five knobs of the manual bar, named instead of carrying a View id. */
public enum ManualParam {
    ISO,
    EXPOSURE,
    EV,
    FOCUS,
    WB
}
