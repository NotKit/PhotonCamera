package com.particlesdevs.photoncamera.circularbarlib.control.knob;

/** What the knob is doing, reported to a {@link KnobChangedListener}. */
public enum RotationState {
    IDLE,
    ROTATING,
    STARTING,
    STOPPING
}
