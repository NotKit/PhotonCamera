/* The Android Gradle plugin generates this from app/build.gradle and
 * app/version.properties; the port has no aapt2, so it is written out here.
 * Keep in step with `versionName` in app/build.gradle and VERSION_BUILD in
 * app/version.properties. */
package com.particlesdevs.photoncamera

object BuildConfig {
    const val APPLICATION_ID: String = "com.particlesdevs.photoncamera"
    const val BUILD_TYPE: String = "release"
    const val DEBUG: Boolean = false
    const val VERSION_NAME: String = "0.97"
    const val VERSION_CODE: Int = 26682
}
