/* The second package j2k star-imports into a converted file (third-party
 * spellings), beside org.mozilla.gecko.knstub.  convert.sh deletes gen/org, so
 * the import needs a package to land on. */
package org.mozilla.gecko.knthird
