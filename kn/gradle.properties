# 4g and no more: this box runs five lane agents at once and konan's LLVM half
# is off-heap, so a bigger JVM heap starves the linker rather than helping it.
org.gradle.jvmargs=-Xmx4g
kotlin.native.ignoreDisabledTargets=true
